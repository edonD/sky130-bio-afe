# Bandgap Voltage Reference

## Purpose

Generate a stable ~1.2V reference voltage and ~1 µA bias current from the 1.8V supply. This is the foundational block — every other analog block depends on it. A 1% drift in V_REF causes 40 LSBs of error in the 12-bit ADC downstream.

## Files

| File | Editable? | Purpose |
|------|-----------|---------|
| `specs.json` | **NO** | Pass/fail targets. Never modify. |
| `program.md` | **NO** | This file. |
| `design.cir` | YES | Your SPICE netlist. Parametric. |
| `parameters.csv` | YES | Parameter names, min, max, scale for optimizer. |
| `evaluate.py` | YES | Simulation runner, measurement extraction, scoring. |
| `best_parameters.csv` | YES | Current best parameter values. |
| `measurements.json` | YES | Output measurements (generated by evaluate.py). |
| `README.md` | YES | **Your final deliverable.** All plots, analysis, design rationale. |
| `plots/` | YES | All generated plots. |
| `results.tsv` | YES | Experiment log (not committed to git). |

**You CANNOT modify:** `specs.json`, `program.md`, SKY130 PDK model files, `../../interfaces.md`, `../../master_spec.json`.

**Critical rule:** Never game the process by editing PDK model files, fabricating simulation results, or tweaking the evaluation to make specs pass artificially. The circuit must actually work.

## Evaluated Parameters

These are read from `specs.json`. Every one must pass.

| Parameter | Target | Weight | What It Means |
|-----------|--------|--------|---------------|
| `vref_v` | 1.15–1.25 V | 20 | Reference voltage at nominal TT/27°C/1.8V |
| `tc_ppm_c` | < 50 ppm/°C | 25 | Temperature coefficient from -40°C to 125°C |
| `psrr_dc_db` | > 60 dB | 20 | How much supply noise is rejected |
| `line_regulation_mv_v` | < 5 mV/V | 10 | V_REF change per volt of supply change |
| `power_uw` | < 20 µW | 15 | Total quiescent power |
| `startup_time_us` | < 100 µs | 10 | Time from power-on to stable V_REF |

## Testbenches

### TB1: DC Operating Point
- Simulate at TT, 27°C, 1.8V. Measure V_REF and supply current.
- **Pass:** V_REF in 1.15–1.25V, power < 20 µW.
- **Plot:** `plots/dc_operating_point.png`

### TB2: Temperature Sweep
- Sweep -40°C to 125°C. Plot V_REF vs temperature.
- Compute TC = (V_max − V_min) / (V_nom × ΔT).
- **Pass:** TC < 50 ppm/°C.
- **Plot:** `plots/vref_vs_temperature.png` — must show the classic bandgap "bow" shape. If it doesn't, the circuit is wrong regardless of the number.

### TB3: Supply Sweep (Line Regulation / PSRR)
- Sweep VDD from 1.62V to 1.98V. Measure V_REF vs VDD.
- Compute PSRR = 20·log10(ΔVDD/ΔV_REF).
- **Pass:** Line regulation < 5 mV/V, PSRR > 60 dB.
- **Plot:** `plots/vref_vs_supply.png`

### TB4: Startup Transient
- Ramp VDD from 0 to 1.8V in 10 µs. Monitor V_REF settling.
- **Pass:** V_REF within 1% of final value in < 100 µs. Must not get stuck at 0V.
- **Plot:** `plots/startup_transient.png`

### TB5: PVT Corner Analysis
- Run TB1+TB2 across all 5 process corners × 3 temperatures × 3 supplies = 45 corners.
- **Pass:** V_REF stays within 1.15–1.25V at ALL corners.
- **Plot:** `plots/pvt_corners.png` — overlay of V_REF(T) for all process corners on one graph.

### TB6: Monte Carlo Mismatch
- 200 Monte Carlo samples at nominal conditions (if mismatch models available in SKY130).
- Report mean, std, worst-case V_REF.
- **Pass:** 3σ spread < 30 mV.
- **Plot:** `plots/monte_carlo_histogram.png`

## How to Evaluate Your Design Honestly

After every simulation run, **look at the plots before trusting the numbers.** A number can pass by accident while the circuit is fundamentally broken.

**For every plot, ask yourself:**
- Does V_REF vs temperature show the classic bow shape? If it's a straight line or monotonic, something is wrong.
- Is V_REF actually near 1.2V? If it's 0.6V or 1.8V, the bandgap principle isn't working.
- Does startup actually converge, or does it get stuck at a metastable point?
- Is power in the µW range? If it's mW, the bias is way too high.
- Is PSRR > 40 dB with bare mirrors? If you see > 80 dB, verify with a cascode or regulated structure — bare mirrors don't give that.
- If any measurement returns exactly 0 or NaN, the simulation setup is wrong. Don't ignore it.

**If something looks weird:**
- Don't hide it. Show the plot in README.md, annotate the anomaly, explain what you think is happening.
- Try a different topology. If a Brokaw bandgap isn't converging, try a Banba. If MOSFETs-only gives poor TC, try using SKY130 parasitic PNPs.
- Honest 45 ppm/°C with good margins beats claimed 5 ppm/°C from a broken simulation.

## Design Freedom

You choose everything about the design:
- **Topology:** Brokaw, Banba, self-biased, sub-1V, MOSFET-only, or anything else.
- **Devices:** SKY130 1.8V MOSFETs (pfet_01v8, nfet_01v8), parasitic PNPs (pnp_05v5), poly resistors, MIM caps.
- **Optimization:** Differential evolution, Bayesian, CMA-ES, manual sweep, or any method. `pip install` anything.
- **Startup circuit:** Required — the degenerate zero-current state must be avoided.

Research freely: web search for "bandgap reference SKY130", look up ISSCC papers, read Razavi's textbook chapter on references, find GitHub repos with similar designs.

## README.md — Your Final Deliverable

This is the only thing I check. After every meaningful improvement, update it with:

1. **Status banner** — score, which specs pass/fail
2. **Spec table** — measured vs target, margin %, pass/fail
3. **All plots** from the testbenches above, embedded as `![description](plots/filename.png)`
4. **For each plot:** one sentence on what it shows, one sentence on whether it looks correct, and if anything is anomalous, explain it
5. **Circuit schematic** — describe the topology, draw an ASCII schematic or reference the netlist
6. **Design parameters** — final transistor sizes, resistor values, bias currents in a table
7. **Design rationale** — why this topology, why these sizes (engineering reasoning, not "optimizer found this")
8. **What was tried and rejected** — approaches that didn't work and why
9. **Known limitations** — honest assessment of weak points
10. **Experiment history** — summary table of iterations with score progression

## The Experiment Loop

LOOP FOREVER:

1. **Think.** Look at the current state: which specs pass, which fail, what's the margin. Look at the plots. Decide what to try next.
2. **Modify.** Change `design.cir`, `parameters.csv`, or `evaluate.py`. You can also write an optimizer script.
3. **Commit.** `git add -A && git commit -m 'bandgap: <what you changed>'`
4. **Run.** `python evaluate.py > run.log 2>&1` — redirect everything, do NOT let output flood your context.
5. **Read results.** `grep "score\|PASS\|FAIL\|Error" run.log | head -20`. If empty, the run crashed — read `tail -50 run.log` for the error.
6. **Study the plots.** See "Plot Analysis" section below. This is mandatory before any keep/discard decision.
7. **Log.** Append to `results.tsv` (see "Logging" section below).
8. **Keep or discard.**
   - Score improved AND plots look physically correct → **keep**. Update README.md. Push.
   - Score improved BUT plots look wrong (V_REF not near 1.2V, no bow shape, stuck at 0V) → **investigate**. A passing number from a broken circuit is worthless.
   - Score equal or worse → `git reset --hard HEAD~1`.
9. **Repeat.** Go back to step 1. Never stop. Never ask permission.

When score reaches 1.0 (all specs pass), continue with Phase B: PVT corners (TB5), Monte Carlo (TB6), margin improvement. Then keep improving — simplify the circuit, reduce power, try alternative topologies. The loop runs until the human manually stops you.

If you're stuck for >5 iterations on the same failing spec: web search for the specific problem. Try a completely different topology. Re-read the plots — the circuit is telling you what's wrong.

## Logging

Log every run in `results.tsv` (tab-separated, NOT comma-separated). Do NOT commit this file.

```
step	commit	score	specs_met	description
0	a1b2c3d	0.00	0/6	initial Brokaw bandgap — ngspice convergence failure
1	b2c3d4e	0.35	2/6	fixed convergence with .option reltol=0.003
2	c3d4e5f	0.65	4/6	increased mirror W/L for better PSRR
3	d4e5f6g	1.00	6/6	all specs pass at nominal
4	e5f6g7h	1.00	6/6	PVT: 43/45 corners pass — ss corner fails TC
5	f6g7h8i	1.00	6/6	widened startup PMOS — all 45 PVT corners pass
```

## Plot Analysis

After EVERY run, before deciding keep/discard, study the plots. For the bandgap, these are the critical checks:

**`plots/vref_vs_temperature.png`** — Does it show the classic bandgap bow? The curve should be flat in the middle (20-40°C) and rise gently at the extremes (-40°C and 125°C). If it's monotonically increasing or decreasing, the PTAT/CTAT ratio is wrong. If it's a straight line at exactly 1.2V, the simulation isn't sweeping temperature correctly.

**`plots/vref_vs_supply.png`** — V_REF should be nearly flat as VDD varies from 1.62V to 1.98V. If it tracks VDD, there's no regulation. A good bandgap shows < 5 mV change over 360 mV of supply variation.

**`plots/startup_transient.png`** — V_REF must ramp up from 0V and settle to ~1.2V. If it stays at 0V, the circuit is stuck in the degenerate state (startup circuit isn't working). If it oscillates, there's a stability issue. The settling should be clean and monotonic.

**`plots/pvt_corners.png`** — All 5 process corner curves overlaid. They should all show the bow shape but shifted vertically. If one corner (e.g., SS) gives a drastically different V_REF, the design is not robust. FF and SS should bracket TT.

**System-level check:** "Will this V_REF be stable enough for the 12-bit ADC downstream? At 12 bits over 1.8V, 1 LSB = 0.44 mV. If V_REF drifts by 10 mV over temperature, that's 23 LSBs of gain error in the ADC." Report this number in the README.

When a plot passes and makes physical sense, embed it in README.md with a one-sentence analysis. When something looks wrong, show the plot anyway, annotate the anomaly, and explain what you'll try next.
